# Public OAuth values are in wrangler.jsonc vars.
# After GitHub OAuth app registration, set the client id:
#   npx wrangler vars set GITHUB_OAUTH_CLIENT_ID "<client-id>"
#
# Set the client secret (encrypted in Cloudflare):
#   npx wrangler secret put GITHUB_OAUTH_CLIENT_SECRET
